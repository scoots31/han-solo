FROM python:3.11-slim

WORKDIR /app

COPY mcp/requirements.txt requirements.txt
RUN pip install --no-cache-dir -r requirements.txt

COPY mcp/ mcp/

EXPOSE 8000

CMD ["python", "-m", "uvicorn", "mcp.server:app", "--host", "0.0.0.0", "--port", "8000"]
